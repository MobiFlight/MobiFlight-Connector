# Club member credentials
TESTS_MEMBER_EMAIL=#Email for test user with member status, needs to be registered in the backend
TESTS_MEMBER_PASSWORD=#Password for member user
TESTS_MEMBER_NAME=#Your expected username

# Basic account credentials
TESTS_BASIC_EMAIL=#Email for test user with basic status, needs to be registered in the backend
TESTS_BASIC_PASSWORD=#Password for basic user
TESTS_BASIC_NAME=#Your expected username

# feed options
VITE_FEED_REMOTE_BASE_URL=# Optional remote feed override. If unset, bundled i18n feed remains the only source.

# profile options
VITE_PROFILE_API_BASE_URL=#optional api endpoint for testing. Using production endpoint if not set